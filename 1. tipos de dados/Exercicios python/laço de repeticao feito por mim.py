import os 
os.system("cls||clear")

nota = 0
opcao = str
contador = 0
soma = 0
media = 0

nota = int(input("Digite uma nota: "))
opcao = str(input("Deseja inserir mais uma nota (S ou N): "))

while opcao :
 nota = int(input("Digite uma nota: "))
 opcao = str(input("Deseja inserir mais uma nota (S ou N): "))

if opcao N or n:
 soma += media
 media = soma/contador